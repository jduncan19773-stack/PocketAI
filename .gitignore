venv/
__pycache__/
pycache/
dist/
build/
*.spec
*.pyc
*.pyo

# USB runtime data — never commit user conversations or cached responses
data/
models/
engines/
bin/

# Knowledge base — commit only the small seed set; the bulk corpus and the
# search index are built locally by build_knowledge.py (too big for git).
knowledge/corpus/
knowledge/index.db
# (knowledge/seed/ IS committed so a fresh clone works immediately)

# Temporary and system files
.env
*.log
*.part
benchmarks/report_*.md

# OS files
.DS_Store
Thumbs.db
desktop.ini
